# polis-embed

Polis Memory embeddings: the Embedder trait, on-device Apple backends (feature `apple`), the vector cache, brute-force cosine search over the store's int8 index, and the bounded index tick.

Part of [Polis Memory](https://github.com/sersiousSenpai/polis-memory) — a local-first memory for coding agents. The README, the docs and the plan live in the repository; this crate is one layer of it.

License: Apache-2.0.
